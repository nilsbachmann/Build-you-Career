# Einfache KI-Logik für NPCs

def npc_decision(game_state):
    # Beispiel: NPC entscheidet zufällig zwischen "arbeiten" und "lernen"
    import random
    return random.choice(["arbeiten", "lernen"])